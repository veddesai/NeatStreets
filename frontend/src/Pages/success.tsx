
const success = () => {
  return (
    <div>success</div>
    //This left.

    // Integrated Mail Service for verifying otp for user registration, now need some feedback for waiting time it takes to send otp mail..
    // After successful signup, need to show that i succesfully signed up.
    
  )
}

export default success